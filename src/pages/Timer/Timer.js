import React from 'react';
import PropTypes from 'prop-types';

const Timer = () => (
  <div className="container pt-4">
    <h2>Timer</h2>
    <p className="text-muted fs4">Pending!</p>
  </div>
);

Timer.propTypes = {};

Timer.defaultProps = {};

export default Timer;
